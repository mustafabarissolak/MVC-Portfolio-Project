@model List<Experience>
@{
    ViewData["Title"] = "Experience";
    Layout = "~/Views/Layout/Index.cshtml";
}
<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">Experiences</h4>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-body">
                        <table class="table mt-3">
                            <thead>
                                <tr style="text-align:center">
                                    <th scope="col">Id</th>
                                    <th scope="col">Tarih</th>
                                    <th scope="col">Şirket Adı</th>
                                    <th scope="col">Departman</th>
                                    <th scope="col">Proje</th>
                                    <th scope="col">Açıklama</th>
                                    <th scope="col">Sil</th>
                                    <th scope="col">Güncelle</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.Id</td>
                                        <td>@item.DateTime</td>
                                        <td>@item.CompanyName</td>
                                        <td>@item.Department</td>
                                        <td>@item.Project</td>
                                        <td>@item.Description</td>
                                        <td><a href="/Experience/Edit/@item.Id" class="btn btn-success">Güncelle</a></td>
                                        <td><a href="/Experience/Delete/@item.Id" class="btn btn-danger">Sil</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a asp-action="Create" class="btn btn-primary">Ekle</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

